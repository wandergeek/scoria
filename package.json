{
  "name": "scoria",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "moment-timezone": "^0.5.33",
    "sentiment": "^5.0.2",
    "twilio": "^3.56"
  },
  "devDependencies": {
    "twilio-run": "^3.2.0"
  },
  "engines": {
    "node": "12"
  }
}
